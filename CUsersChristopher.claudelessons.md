
- [2026-06-25] (jester-researcher) HARD RULE — NO PAID APIs: proposed TCGPlayer OAuth as primary option when user said "I will never pay for things like this" → root cause: forgot to check memory/CLAUDE.md for cost stance → RULE: when fetching market data, default order: (1) free public APIs, (2) scraping via proxies + Playwright (free but slower), (3) derived from existing data. Never suggest paid tiers/OAuth/subscriptions as primary path; only mention if user explicitly asks tradeoff. User's cost bias = default scraper-first mentality.

- [2026-06-25] (jester-researcher) tcgcsv API endpoints unreliable/broken (XML responses, JSON parse failures) — RULE: NEVER scrape tcgcsv via API. CSV files are the source: pull Categories.csv → parse → map to database. CSV is stable, deterministic, no JSON/parsing overhead. Build CSV chain: Categories.csv (categoryId) → Groups.csv (groupId per category) → Products.csv (individual cards + market prices per group). Fetch daily after 20:00 UTC update.
